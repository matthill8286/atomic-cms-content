query MultiColumnText($id: ID!) {
  multiColumnText(where: {id: $id}, stage: DRAFT) {
    type: __typename
    columnFourDescription
    columnFourTitle
    columnOneDescription
    columnOneTitle
    columnThreeDescription
    columnThreeTitle
    columnTwoDescription
    columnTwoTitle
    id
    numberOfColumns
  }
}
